function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);

  // initialisierung; bedingung; update

  for (let i = 0; i < 10; i++) {

       ellipse(200 *i, height / 2, 200, 200);

    console.log(i);

    

    //anweisung


  }
}
